# this is a comment in application.properties
# property.subproperty = value
# you can configure any application specific properties like datasource, server port, 
# server context path, microservice configuration and so on
server.port=9999
server.servlet.context-path=/first-app
spring.application.name=firstapp
#auth-server=http://localhost:8181/authserver
#security.oauth2.client.id=my-client
#security.oauth2.client.client-secret=password
#
#security.oauth2.client.scope=ADMIN
#security.oauth2.client.access-token-uri=${auth-server}/oauth/token
#security.oauth2.client.user-authorization-uri=${auth-server}/oauth/authorize
#security.oauth2.resource.user-info-uri=${auth-server}/admin
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/grocers
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=create-drop
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
